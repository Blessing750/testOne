// string interpolation is an easier way of concantination
/* const text = `He's often called "Johnny"`;
document.write(text)

// Template literals allows multiline strings
const text2 = `<br> The quick
brown fox
jumps over
the lazy dog`;
document.write(text2)

const header = "Templates Literals";
const tags = ["template literals", "javascript", "es6"];

const html = `<h2>${header}</h2><ul>`;
for (const x of tags) {
  html += `<li>${x}</li>`;
}

html += `</ul>`;
document.write(header)

const firstName = "John";
const lastName = "Doe";

const text1 = `<br> Welcome ${firstName}, ${lastName}!`;
document.write(text1)*/